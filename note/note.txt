note cmd: 
sudo npm install -g @angular/cli
sudo npm install -g angular-cli-ghpages

sudo apt-get install ibus-unikey
ng build --localize
ng serve --configuration=vi   
ng extract-i18n 

sudo apt update
sudo apt install ibus-unikey
ibus restart


find src/app -type f -name "*.ts" -exec sed -i '/BrowserModule/d' {} +
grep -rn "BrowserModule" src/app


.pipe(
  tap(...),           // side effects
  map(...),           // xử lý giá trị
  catchError(...),    // xử lý lỗi sớm
  takeUntil(...),     // đảm bảo hủy khi destroy
  finalize(...)       // cleanup cuối cùng
)


bin/kafka-console-producer.sh --topic register-topic --bootstrap-server localhost:9092
