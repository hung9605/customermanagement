note cmd: 
sudo npm install -g @angular/cli
sudo npm install -g angular-cli-ghpages

sudo apt-get install ibus-unikey
ng build --localize
ng serve --configuration=vi   
ng extract-i18n 

sudo apt update
sudo apt install ibus-unikey
ibus restart


find src/app -type f -name "*.ts" -exec sed -i '/BrowserModule/d' {} +
grep -rn "BrowserModule" src/app


.pipe(
  tap(...),           // side effects
  map(...),           // xử lý giá trị
  catchError(...),    // xử lý lỗi sớm
  takeUntil(...),     // đảm bảo hủy khi destroy
  finalize(...)       // cleanup cuối cùng
)


bin/kafka-console-producer.sh --topic register-topic --bootstrap-server localhost:9092


bin/kafka-console-consumer.sh --topic SUPPLIES_TOPIC --from-beginning --bootstrap-server localhost:9092

bin/kafka-topics.sh --bootstrap-server localhost:9092 
  --create --topic registerregister-topic 
  --partitions 1 \
  --replication-factor 1

bin/kafka-server-start.sh config/kraft/server.properties

--reset offset to lastestlastest
bin/kafka-consumer-groups.sh --bootstrap-server localhost:9092 --group console-consumer-74131 --reset-offsets --to-latest --all-topics --execute


bin/kafka-consumer-groups.sh --bootstrap-server localhost:9092   --group inventory-service-group   --topic SUPPLIES_TOPIC   --reset-offsets --to-earliest --execute
reset offset = 0


-- check chi tiet topic 
./kafka-configs.sh --bootstrap-server localhost:9092 --entity-type topics --entity-name SUPPLIES_TOPIC --describe


show key
SELECT CONSTRAINT_NAME, TABLE_NAME, COLUMN_NAME
FROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE
WHERE TABLE_NAME = 'inventory'

mvn install:install-file \
  -Dfile=/home/user/project/javaplugin/target/JavaPlugin-1.jar \
  -DgroupId=JavaPlugin \
  -DartifactId=JavaPlugin \
  -Dversion=1 \
  -Dpackaging=jar

~/Downloads/apache-maven-3.9.10-bin/apache-maven-3.9.10/bin mvn clean install

~/Downloads/apache-maven-3.9.10-bin/apache-maven-3.9.10/bin/mvn clean compile
